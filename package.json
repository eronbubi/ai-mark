{
  "name": "ai-mark",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Mark - Benchmark-Index fuer Sprachmodelle. Statische Seite ohne Build-Schritt.",
  "scripts": {
    "dev": "npx --yes serve . -l 3000",
    "deploy": "npx --yes vercel deploy --prod"
  }
}
